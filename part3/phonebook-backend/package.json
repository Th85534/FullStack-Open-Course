{
  "name": "phonebook-backend",
  "version": "1.0.0",
  "main": "index.js",
  "scripts": {
    "start": "node index.js",
    "dev": "nodemon index.js",
    "build:ui": "rm -rf dist && cd ../ && cd ../ && cd part2/phonebook && npm install && npm run build && cp -r dist ../../part3/phonebook-backend",
    "deploy": "git config user.email 'dasguptasoumasish@gmail.com' && git config user.name 'Th85534' && npm run build:ui && cd ../../part3/phonebook-backend && git add . && git commit -m 'uibuild' && git push",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Soumasish",
  "license": "ISC",
  "description": "",
  "dependencies": {
    "cors": "^2.8.5",
    "express": "^4.21.2",
    "morgan": "^1.10.0"
  },
  "devDependencies": {
    "nodemon": "^3.1.9"
  }
}
